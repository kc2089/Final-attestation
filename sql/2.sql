CREATE TABLE Pets (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    Name VARCHAR(50),
    Type VARCHAR(20),
    BirthDate DATE,
    Commands VARCHAR(255)
);

CREATE TABLE PackAnimals (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    Name VARCHAR(50),
    Type VARCHAR(20),
    BirthDate DATE,
    Commands VARCHAR(255)
);
